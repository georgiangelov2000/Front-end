<div class="user-card">
    <h1>Id:{{id}}</h1>
    <h2>Name: {{name}}</h2>
    <img src="{{imgSrc}}" alt="">
    <button class="detailsBtn">Details</button>
    <div class="info">
            <p>Phone number: {{phoneNumber}}</p>
            <p>Email: {{email}}</p>
    </div>
</div>